ShadowBound — Мрачный 2D Souls-like экшен от одного разработчика

«Тени не убивают — они лишь ждут, пока ты устанешь бороться…»
ShadowBound — это мрачный 2D экшен-платформер с элементами RPG и roguelike, созданный полностью в одиночку: код, пиксель-арт, звуки, UI, сюжет — всё сделано мной. Игра вдохновлена Dead Cells, Hollow Knight и Salt and Sanctuary, но предлагает собственный взгляд на боевую глубину, прогрессию и атмосферу.
🔥 Что вас ждёт?
⚔️ Тактическая боевая система уровня Souls-like
Frame-perfect парирование с замедлением, тряской камеры и тактильной отдачей
Комбо-механика через концентрацию: каждый успешный удар восстанавливает концентрацию и усиливает урон (мультипликатор 1 + hits/10)
Множественные типы ударов: пронзающий (Pierce), рубящий (Slash), полукруговой (SemiCircleSlash), круговой (Circle) — каждый с уникальной геометрией рейкастов
Удары в спину и экзекуции: кинематографичные добивания на последнем издыхании
Магия и амулеты: огненные шары, вампиризм, контр-урон и другие способности
❤️ Двойная система здоровья с кровотечением
Текущее здоровье (tempHealth) и базовое здоровье (health) — реализация механики «кровотечения после боя»
Реактивное управление состоянием: IReadOnlyReactiveProperty<HealthState> для мгновенного отклика систем (аниматор, камера, звук)
Визуальная обратная связь: тряска камеры при уронах >20% от максималки, следы крови с пулингом частиц
🩸 Оптимизированные визуальные эффекты
Пул крови и следов: предварительно инстанцированные объекты с автоматическим возвратом в пул через корутины
Адаптивная интенсивность: количество частиц пропорционально урону (Emit(Mathf.CeilToInt(damage / 2f)))
Случайные вариации следов: спрайты и цвета выбираются из массивов для органичности
👁️ Адаптивный ИИ и мир
Многоуровневое восприятие: враги различают зоны (дальняя патрулирования, ближняя атаки, координация с союзниками)
Рискованные ставки: играйте и испытывайте удачу — проигрываете души или получите больше
📱 Мобильная оптимизация без компромиссов
60 FPS даже на mid-range Android благодаря пулингу и объектному пулу
Отзывчивое управление: D-pad + кнопки внизу экрана
Адаптивный UI: масштабируется под любой экран
🛠 Архитектурные решения высокого уровня
1. Единая система боя через абстракции (CombatSystem.cs)
csharp
12345678910111213141516171819202122232425
// Геометрия ударов как данные — не хардкод
private Dictionary<SlashType, Vector2[]> _slashVectors = new()
{
   { SlashType.Pierce, new[] { new Vector2(1, 0) } },
   { SlashType.Slash, new[] { new Vector2(0, 1), new Vector2(1, 1), new Vector2(1, 0)} },
   { SlashType.SemiCircleSlash, new[] { new Vector2(-1, 0), new Vector2(-1, 1), new Vector2(0, 1), new Vector2(1, 1), new Vector2(1, 0)} },
   { SlashType.Circle, new[] {new Vector2(0, -1), new Vector2(-1, -1), new Vector2(-1, 0), new Vector2(-1, 
Философия: «Каждый может повредить каждого» — через интерфейс IDestructable, а не через проверку тегов. Это позволяет магии, ловушкам и даже окружению наносить урон без костылей.
2. Реактивная система здоровья с кровотечением (BaseHealth.cs)
csharp
123456789101112131415161718192021222324252627
// Реактивные свойства вместо публичных полей
protected readonly ReactiveProperty<float> _currentHealth = new();
protected readonly ReactiveProperty<float> _currentTempHealth = new();
protected readonly ReactiveProperty<HealthState> _state = new(HealthState.Alive);

// Кровотечение как плавный переход между состояниями
protected virtual void HandleTempHealthBleeding()
{
    if (_currentTempHealth.Value <= _currentHealth.Value) return;
    

Преимущество: Состояние здоровья становится источником истины для всей системы — аниматор реагирует на HealthState, камера трясётся при критическом уроне, UI обновляется без ручных вызовов SetHealth().
3. Оптимизированный пул крови (BloodEffectsPoolManager.cs)
csharp
1234567891011121314151617181920
// Предварительное создание пула в Awake() — ноль аллокаций в бою
private void Awake()
{
    for (int i = 0; i < 5; i++)
    {
        GameObject blood = Instantiate(bloodParticlesPref, Vector2.zero, Quaternion.identity, transform);
        _container.InjectGameObject(blood); // Zenject для зависимостей
        bloodParticlesPool.Push(blood);
        blood.SetActive(false);
    }

Экономия: На мобильных устройствах это критично — 5 предварительно созданных объектов вместо 50+ аллокаций в секунду при массовых боях. Кадровые просадки уходят в ноль.
📂 Структура проекта
123456789101112
Assets/
├── Animations/       # Анимации на костях (bone rigging)
├── Art/              # Пиксель-арт: герои, враги, окружение
├── Prefabs/          # Префабы с преднастроенными системами
├── Scenes/           # Уровни на Tilemap
├── Scripts/
│   ├── Combat/       # CombatSystem, BaseHealth, IDestructable
│   ├── AI/           # BaseEnemyAI, EnemyAIWarrior
│   ├── VFX/          # BloodEffectsPoolManager, CharacterVFXManager
│   ├── Core/         # UniRx реактивные свойства, Zenject биндинги

💡 Почему это работает на мобильных устройствах?
Проблема
Решение в ShadowBound
Аллокации в бою
Пул крови + объектный пул для врагов
Слишком много рейкастов
Кэширование геометрии ударов в Dictionary<SlashType, Vector2[]>
Тормоза от анимаций
Bone rigging вместо покадровой анимации
Утечки памяти
Автоматическая отписка через CompositeDisposable.AddTo(this)
Непредсказуемый ИИ
Стейт-машина с реактивными переходами (DistinctUntilChanged())
📬 Контакты
Если вас заинтересовал мой опыт или вы хотите обсудить сотрудничество — пишите!
Telegram: @diiimaaan777
💡 Полная игра доступна на itch.io
Полный исходный код предоставляется по запросу — с комментариями на английском, как ты предпочитаешь.
ShadowBound — не просто игра. Это доказательство того, что сложные системы можно создать в одиночку, если архитектура продумана до мелочей. Каждый рейкаст, каждый пул, каждая реактивная подписка — всё работает как часы. Даже когда герой умирает в сотый раз. 😄
